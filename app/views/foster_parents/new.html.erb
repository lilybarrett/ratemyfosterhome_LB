<h1>Add Foster Parent Information</h1>

<%= form_for @foster_parent do |f| %>
    <div class="field">
      <%= f.label :first_name, "First Name" %>
      <%= f.text_field :first_name %>
    </div>

    <div class="field">
      <%= f.label :last_name %>
      <%= f.text_field :last_name %>
    </div>

    <div class="actions">
      <%= f.submit "Add Foster Parent Information", class: "button round submit" %>
    </div>
<% end %>
